apply plugin: 'java'
apply plugin: 'application'

sourceCompatibility = 1.7
version = '1.0'
mainClassName = "de.rsi.Validator"

repositories {
    mavenCentral()
}

dependencies {
    compile 'info.cukes:cucumber-core:1.1.5'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}